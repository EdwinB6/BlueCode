import { CursorBlinkDirective } from './cursor-blink.directive';

describe('CursorBlinkDirective', () => {
  it('should create an instance', () => {
    const directive = new CursorBlinkDirective();
    expect(directive).toBeTruthy();
  });
});
